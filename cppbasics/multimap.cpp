#include <bits/stdc++.h>
using namespace std;

int main()
{ 
  // all the keys in sorted order always 
  // can store multiple key value pairs 
  // like object,dulpicate keys
  //only map[key]  cannot be used here
    multimap<int ,int >Mumap;
    // log(n)


    unordered_multimap<int,int >un;
    // o(1)

  return 0;
}